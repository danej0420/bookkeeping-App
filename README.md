# bookkeeping-App

Starter bookkeeping web app for self-employed hair stylists. The frontend lives in `frontend/` (Next.js + TypeScript) and the backend lives in `backend/` (Express + Prisma + PostgreSQL).
